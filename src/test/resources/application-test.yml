spring:
  datasource:
    username: root
    password: password
  liquibase:
    contexts: ddl

  jpa:
    properties:
      hibernate:
        enable_lazy_load_no_trans: true
        format_sql: false
        show_sql: false

dluhc:
  request.header.name: client-cert-serial

api:
  ier:
    base.url: http://wiremock
    sts.assume.role: arn:aws:iam::1234567890987:role/grant-me-access-to-ier
